import React from "react";

function OrderPage() {
    return (
        <div>this is the order page that shows the user's order summary and they can pay for the order</div>
    );
}

export default OrderPage;